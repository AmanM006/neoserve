# NeoServe harness dependencies.
# The harness itself (sweep/stats/economics/reporting) runs anywhere, including
# Windows/macOS in --mock mode. The heavy inference deps are only needed on the
# Graviton4 target and are installed there by deploy/ec2-setup.sh.

# --- Core harness (needed everywhere, incl. mock mode) ---
numpy>=1.26
pyyaml>=6.0
rich>=13.7
typer>=0.12
jinja2>=3.1
httpx>=0.27

# --- Reporting / plots (optional but recommended) ---
matplotlib>=3.8

# --- MCP server (optional) ---
mcp>=1.2.0

# --- Inference + quantization (Graviton4 target only; see deploy/ec2-setup.sh) ---
# Installed on the Arm host, pinned there for reproducibility:
#   vllm>=0.11.0           # aarch64 CPU backend (oneDNN + Arm Compute Library)
#   llmcompressor>=0.4.0   # W8A8 / W4A8 quantization recipes
#   transformers>=4.45
#   datasets>=3.0
#   lm-eval>=0.4.5         # quality guard (perplexity / task accuracy)
